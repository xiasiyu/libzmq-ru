# ABI Tests

Rust ABI tests currently live in `crates/libzmq-ffi/tests` so Cargo can run them for the C ABI crate.

This directory tracks the logical test category for agents and future cross-crate test assets.
